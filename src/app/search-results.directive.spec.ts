import { SearchResultsDirective } from './search-results.directive';

describe('SearchResultsDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchResultsDirective();
    expect(directive).toBeTruthy();
  });
});
